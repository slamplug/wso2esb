<datasource>
            <name>WSO2_SHARED_DB</name>
            <description>The datasource used for User Management database</description>
            <jndiConfig>
                <name>jdbc/WSO2_SHARED_DB</name>
            </jndiConfig>
            <definition type="RDBMS">
                <configuration>
                    <url>jdbc:oracle:thin:@(DESCRIPTION = (ADDRESS_LIST = (ADDRESS = (PROTOCOL = TCP) (HOST = wso2-des-db1) (PORT = 1521)) (ADDRESS = (PROTOCOL = TCP) (HOST = wso2-des-db2) (PORT = 1521)) (FAILOVER=OFF) (LOAD_BALANCE=ON)) (CONNECT_DATA = (SERVER = dedicated) (SERVICE_NAME = des_ctrck.isit0)))</url>
                    <username>desesb_app</username>
                    <password>appuser</password>
                    <defaultAutoCommit>false</defaultAutoCommit>
                    <driverClassName>oracle.jdbc.driver.OracleDriver</driverClassName>
                    <maxActive>50</maxActive>
                    <maxWait>60000</maxWait>
                    <testOnBorrow>true</testOnBorrow>
                    <validationQuery>SELECT 1 FROM DUAL</validationQuery>
                    <validationInterval>30000</validationInterval>
                </configuration>
            </definition>
        </datasource>